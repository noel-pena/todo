# Server Configuration
server.port=8080

# Spring Data MongoDB Configuration
spring.data.mongodb.uri=mongodb+srv://noelpena:1234@cluster0.xbaexmf.mongodb.net/?retryWrites=true&w=majority&appName=Cluster0
spring.data.mongodb.ssl.enabled=true
spring.data.mongodb.auto-index-creation=true

# Logging Configuration
logging.level.org.springframework=INFO
logging.level.com.example=DEBUG

# CORS Configuration
spring.web.cors.allowed-origins=*
spring.web.cors.allowed-methods=GET,POST,PUT,DELETE,OPTIONS
spring.web.cors.allowed-headers=*
spring.web.cors.allow-credentials=true

# MongoDB Connection Pool Configuration
spring.data.mongodb.connectionPoolMaxSize=100
spring.data.mongodb.connectionPoolMinSize=10

# Application-specific Settings (if needed)
# spring.application.name=todolist-app